# Prompt: Review the Learning Roadmap

Use the `course-validator` skill in Status mode.

Read `roadmap.md`, all existing course specs, and the learning ledger. Report:

- what has been validated;
- what is currently active or in remediation;
- the next required course;
- any inconsistent state;
- the single most useful next action.

Run `python scripts/validate_learning_state.py`. Do not modify files unless I
explicitly ask you to repair an inconsistency.
